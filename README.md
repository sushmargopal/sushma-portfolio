# Sushma R Portfolio

A responsive single-page portfolio for Sushma R, Senior Technical Trainer.

## Run locally

Open `index.html` directly or serve the directory:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

## Deployment

The site is static and can be hosted directly with GitHub Pages from the `main` branch.
